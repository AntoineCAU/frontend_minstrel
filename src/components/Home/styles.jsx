import styled from 'styled-components';

const SHome = styled.div`
  .home {
    margin-top: 80px;
    .mainpix {
      width: 100vw;
      filter: opacity(70%);
      object-fit: cover;
    }
  }
`;

export default SHome;
